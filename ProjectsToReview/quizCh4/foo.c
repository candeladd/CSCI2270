#include <stdio.h>
#include <stdlib.h>

int main()
{
    int addOrSub(int x, int y)
    {
        if (x<y)
        {
            x+y;
        }
        else
        {
            x-y;
        }
    }
}
